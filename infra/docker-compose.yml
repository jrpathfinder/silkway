services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: central_asia
      POSTGRES_USER: central_asia
      POSTGRES_PASSWORD: local_dev_only
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
volumes:
  postgres-data:
