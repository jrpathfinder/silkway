{
  "name": "central-asia-restaurant-platform",
  "private": true,
  "workspaces": ["apps/api", "apps/admin", "packages/contracts"],
  "scripts": {
    "api:build": "npm --workspace apps/api run build",
    "api:test": "npm --workspace apps/api run test",
    "admin:build": "npm --workspace apps/admin run build",
    "contracts:build": "npm --workspace packages/contracts run build"
  }
}
